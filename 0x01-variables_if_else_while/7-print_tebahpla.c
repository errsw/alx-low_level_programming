#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
*/

int main(void)
{
	char alphabet;

	for (alphabet = 'z'; alphabet >= 'a'; c--)
	{putchar(c);
		c--;
	}
	putchar('\n');
	return (0);
}
