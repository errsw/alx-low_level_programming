#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
*/

int main(void)
{
	char alphabet;

	for (alphabet = 'a' ; alphabet <= 'z'; ch++)
		putchar(alphabet);

	for (alphabet = 'A' ; alphabet <= 'Z'; ch++)
		putchar(alphabet);

	putchar('\n');
	return (0);
}
