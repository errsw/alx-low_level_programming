#include <stdio.h>
#include <math.h>

/**
 * main - the largest prime
 * Return: 0
 */

int main(void)
{
	long a, x
	long n = 612852475143;

	for (a = 1; a <= square; a++)
	{
		if (n % a == 0)
		{
			x = n / a;
		}
	}
	printf("%ld\n", x);
	return (0);
}
