#include "main.h"

/**
 * main - Entry point
 * Return: 0
 */

int main(void)
{
	int x = 1, y = 2, z;
	printf("%d, %d, ", x, y);

	for ( int a = 2; a < 50; a++)
	{
		z = x +y;
		printf("%d, ", z);
		i = j;
		j = z;
	}
	printf("\n");
	return(0);
}
